# 服务器购买

https://www.runoob.com/linux/linux-cloud-server.html

# 个人小站点配置服务器

```shell
Ubuntu-cn-south-1-a0Di 运行中  华南-广州

Ubuntu 22.04 server 64bit
2核 | 2GiB | 系统盘 40GiB | 流量包 100GB | 峰值带宽 2Mbit/s
公网IP：
113.45.29.83
私有IP：
192.168.3.95
```

### 流量包 100GB 是什么意思？

**含义：**

这是指你的服务器在一个月内允许传输的数据总量（包括上传和下载）。

- 双向计算：

   它通常包含两个方向的流量：

  - **流出流量（Outbound）：** 服务器发给访客的内容（如网页加载、视频流、API 接口响应）。**这是最容易消耗流量包的部分。**
  - **流入流量（Inbound）：** 访客上传到服务器的内容（如提交表单、上传图片、SSH 远程连接数据）。

- **单位：** GB (Gigabyte，吉字节)。

**对你的影响：**

- 够用吗？

   对于 2 核 2G 的配置，100GB / 月 属于够用但需节省的范围。

  - **适用场景：** 个人博客、小型企业官网、开发测试环境、爬虫（需控制频率）、作为网盘同步少量文件。
  - **不适用场景：** 视频流媒体网站、大型游戏服务器、提供大文件下载的网站。

- 超标后会怎样？

  - 通常云服务商（如阿里云、腾讯云、华为云等）会在流量用尽后，**自动停止网络服务**（你的网站打不开，SSH 连不上），直到下个月重置或你手动购买更多流量包。
  - 少数服务商可能会转为按超额流量计费（扣费）。

### 峰值带宽 2Mbit/s 是什么意思？

**含义：**

这是指你的服务器网卡允许通过的**最高瞬间速度**。

- **单位注意：** 这里的单位是 **Mbit** (Megabit，兆比特)，而不是我们平时下载文件看到的 **MB** (Megabyte，兆字节)。

- **换算公式：** 8 Mbit=1 MB

- 实际速度：

  2 Mbit/s÷8=0.25 MB/s

  也就是说，你的服务器对外传输数据的最高速度大约是256 KB/s。

**对你的影响：**

- **网站加载速度：** 256 KB/s 的速度非常慢。如果你的网页首页大小是 1MB，用户打开它需要 **4 秒** 以上。这对于现代动态网站体验较差，但对于静态博客或后台管理系统是可以接受的。
- **文件传输：** 如果你通过 SFTP 或 SCP 从服务器下载一个 100MB 的文件，理论上最快需要 **400 秒（约 6-7 分钟）** 才能下完。
- **“峰值” 的意思：** 只要不超过 2Mbit/s，速度是不限时的。但如果你的应用瞬间需要 10Mbit/s 的速度，它会被强制限制在 2Mbit/s，导致请求排队或延迟。

### 规格：2 核 | 2GiB（服务器的 “大脑 + 内存”）

#### 1. 通俗解释：

- **2 核**：指服务器的 CPU（中央处理器）核心数，相当于 “工人数量”——2 个工人同时干活。
- **2GiB**：指服务器的内存（RAM），相当于 “工人的工作台大小”—— 工作台越大，能同时摆放的工具（运行的程序、数据）越多。

### 云硬盘：40GiB (系统盘) 高 IO（服务器的 “硬盘”）

#### 1. 通俗解释：

- **40GiB**：存储容量（1GiB≈1G），相当于 “文件柜大小”—— 用来装系统、软件、你的 FastAdmin 项目代码、数据库数据。
- **系统盘**：这是服务器的 “主硬盘”，必须有，用来安装操作系统（比如后面的 Ubuntu），不能删除。
- **高 IO**：硬盘的读写速度（IO = 输入输出），“高 IO”≈“高速硬盘”，比普通硬盘快，适合运行数据库（比如 MySQL）、频繁读写文件的场景。

#### 2. 开发场景关联：

- 40GiB 足够用：Ubuntu 系统占 10-15GiB，FastAdmin 项目 + MySQL 数据库占 5-10GiB，剩余空间足够存放日志、备份文件。
- 高 IO 很重要：FastAdmin 依赖 MySQL 数据库，查询数据时需要硬盘快速读写，“高 IO” 能让你的管理系统页面加载更快、数据查询更流畅。
- 扩展：如果以后项目变大（比如数据量超 20GiB），可以额外加 “数据盘”（相当于额外的文件柜），不用换系统盘。

### 镜像信息：Ubuntu 22.04 server 64bit（服务器的 “操作系统”）

#### 1. 通俗解释：

- 镜像 = 操作系统的 “安装包”，云服务器启动时会自动安装这个系统，相当于给服务器装 “Windows” 或 “MacOS”。
- **Ubuntu**：一个流行的 Linux 系统（开源、免费、稳定，适合服务器）；
- **22.04**：系统版本号（LTS 长期支持版，支持 5 年，不用频繁升级）；
- **server**：服务器版（没有图形界面，只有命令行，占用资源少，适合远程操作）；
- **64bit**：64 位系统（支持大内存，能运行 PHP 7+、MySQL 8 + 等新版本软件，和 FastAdmin 完全兼容）。